#include <climits>
#include <string>
#include <unordered_map>
#include <vector>

using namespace std;

/**
76. 最小覆盖子串
*/

class Solution {
public:
  bool check(vector<int> &flag, vector<int> &test) {
  }

  // s = abc, t=a

  string minWindow(string s, string t) {
  }
};