#include <utility>
#include <vector>

using namespace std;

class Solution {
public:
  vector<int> sortArray(vector<int> &nums) {
  }

private:
  void selectionSort(vector<int> &nums) {
  }
};

int main() {
}
