#include <vector>

using namespace std;

class NumArray {
public:
  NumArray(vector<int> &nums) { (void)nums; }

  int sumRange(int left, int right) {
    (void)left;
    (void)right;
    return 0;
  }
};
