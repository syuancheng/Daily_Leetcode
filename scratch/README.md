# Scratch

This directory is for local draft LeetCode solutions, demos, and quick experiments.

Files here are ignored by Git by default. When a solution is ready, copy or move it
into the canonical location under `problems/<topic>/`, add the matching Markdown
writeup, and update the root `README.md` index.
